[package]
name = "flouze"
version = "0.1.0"
authors = ["Adrien Bustany <adrien@bustany.org>"]

[dependencies]
bytes = "0.4"
prost = "0.2"
prost-derive = "0.2"
uuid = {version = "0.6", features = ["v4"]}
error-chain = "^0.12.0"
sled = "^0.15.0"

[build-dependencies]
prost-build = "0.2"
